# 2026-08-25 改动日志

## Rust 环境搭建 + Tauri 启动验证

- **改了什么**：装 rustup + stable 1.98.0（rustc/cargo）；`~/.zshrc` 追加 `. "$HOME/.cargo/env"` 持久化 PATH；`pnpm-workspace.yaml` 的 `allowBuilds: esbuild: false` 改为 `true` 放行 esbuild 构建脚本。
- **为什么**：机器原先无 Rust，Tauri 后端必须 Rust 工具链才能编译；esbuild 构建脚本被 pnpm 默认拦下，不放行 vite 起不来。`~/.profile` 为 root 拥有写不进，改走 zsh 用户的 `~/.zshrc`。
- **验证结果**：`rustc --version` = 1.98.0 ✅；`cargo --version` = 1.98.0 ✅；`cd src-tauri && cargo build` Finished in 59.05s ✅；`pnpm tauri dev` vite ready + nook 窗口弹出 + localhost:1420 HTTP 200 ✅。环境搭建成功。

## PRD 评审修订（v1.1）

- **改了什么**：`PRD产品需求文档.md` 七处修订。① 建表 SQL 补 `PRAGMA foreign_keys = ON` + `journal_mode = WAL`；② `visualization`/`action_step`/`manifestation_log` 三子表补 `FOREIGN KEY (order_id) REFERENCES cosmic_order(id) ON DELETE CASCADE`；③ 数据库操作规范补"每次连接必须开外键""时间字段存本地时间""导入文件校验魔数头而非后缀""删除订单级联清理子记录"；④ 异常表强化导入校验描述；⑤ 打包产物表补 macOS Apple Silicon（aarch64）产物；⑥ 安装包体积预算调整（exe ≤30MB / dmg ≤40MB）；⑦ 多实例问题标注 V1.1 引入 single-instance 插件并列入迭代规划。文首加「文档修订记录」表。
- **为什么**：原 PRD 正文承诺级联删除，但 DDL 子表无外键约束 + SQLite 默认关外键，级联形同虚设——会在 V1 开发中段暴露"删订单留孤儿数据"；macOS 漏 arm64 产物（开发机就是 Apple Silicon）；体积预算 25MB 偏紧（Vditor 本体大）；导入校验仅看后缀太弱。
- **验证结果**：文档级修订，无需构建验证。改动落盘经核对。

## harness 工程化落地

- **改了什么**：参照 `hbrb-aigc-frontend` 标准范式，为 nook 搭建全套 harness：`AGENTS.md`（路由器式，14 条硬约束按 Vue/Element Plus/Tauri 栈改写）+ `CLAUDE.md`（自包含指针）+ `feature_list.json`（12 个 feature 对齐 PRD V1）+ `feature_list.schema.json` + `init.sh`（pnpm install/build + cargo check + check-records）+ `scripts/check-records.sh`（适配 src/ 含 .vue + src-tauri/src 含 .rs）+ `progress.md`（英文 Current State 头）+ `session-handoff.md` + `logs/2026-08-25.md`（本文件）+ `rules/`×4（project-structure / tauri-ipc / db / style）。
- **为什么**：用户要求参照 hbrb-aigc-frontend 搭建标准 harness，让 coding agent 在 nook 里能顺利起步、守范围、做完能验证、跨会话能续。把 PRD 的 10+ 功能项落成 feature_list，下次会话不靠聊天记录就能接着干。
- **验证结果**：文件结构完整。`./init.sh` 待 feat-001 装完依赖后可全绿跑通（当前 `pnpm build` 在空模板上可过，但 plugin-sql 等依赖未装）。

## feat-001 项目依赖与脚手架扩充

- **改了什么**：
  - **Tauri 插件安装**（`pnpm tauri add`）：`plugin-sql`、`plugin-dialog`、`plugin-fs`。三者在 `src-tauri/Cargo.toml` 加为依赖、`src-tauri/src/lib.rs` 注册（`.plugin(tauri_plugin_sql::Builder::new().build())` 等）、`src-tauri/capabilities/default.json` 授权（`sql:default` / `dialog:default` / `fs:default`）。
  - **关键修复**：`tauri add sql` 自动写入的 `tauri-plugin-sql = "2"` 缺 `features = ["sqlite"]`，无 SQLite 驱动编译不过。手改为 `tauri-plugin-sql = { version = "2", features = ["sqlite"] }`。
  - **前端依赖**（`pnpm add`）：`element-plus@2.14.5`、`@iconify/vue@5.0.1`、`vditor@3.11.3`、`pinia@4.0.3`、`vue-router@5.2.0`（vue-router 4 的 latest tag 解析到 5）、`@vueuse/core@14.4.0`。**构建插件**：`unplugin-vue-components@32.1.0`、`unplugin-auto-import@21.1.0`（Element Plus 按需自动引入）。
  - **`vite.config.ts`**：加 `AutoImport({ resolvers: [ElementPlusResolver()] })` + `Components({ resolvers: [ElementPlusResolver()] })` 插件；加 `@` → `src` 路径别名（`fileURLToPath(new URL("./src", import.meta.url))`）。
  - **`tsconfig.json`**：加 `"baseUrl": "."` + `"paths": { "@/*": ["src/*"] }`；`include` 加 `auto-imports.d.ts`、`components.d.ts`（vue-tsc 能识别自动引入的类型）。
  - **`src/main.ts`**：改写为 `import App from "@/App.vue"` + `import { pinia } from "@/stores"` + `import router from "@/router"` + `createApp(App).use(pinia).use(router).mount("#app")`。
  - **新增文件**：`src/stores/index.ts`（`export const pinia = createPinia()`）、`src/router/index.ts`（占位 home 路由指向 HomeView）、`src/views/HomeView.vue`（脚手架验证页，含 `<el-button>` 证明 Element Plus 按需引入可用）。
  - **`src/App.vue`**：改写为 `<RouterView />` + 白色 `:root` 背景，删模板自带深色 `@media (prefers-color-scheme: dark)` 段（硬约束 #10 禁深色）。
  - **生成类型声明**：`auto-imports.d.ts`、`components.d.ts`（vite build 时由 unplugin 生成，声明 `ElButton` 等供 vue-tsc）。
- **为什么**：feat-001 是全部业务功能的前置——PRD 指定的 SQLite/对话框/文件插件、Element Plus/Iconify/Vditor/Pinia/Vue Router/VueUse 都得先装好、配好别名和按需引入，后续 feat-002~012 才有地基。sqlite feature 漏装是 `tauri add` 的已知坑，必须手补。
- **验证结果**：
  - `pnpm build`（`vue-tsc --noEmit && vite build`）：1619 modules transformed，4.39s，✅ 无类型错误。
  - `cd src-tauri && cargo check`：sqlx 0.8.6 + tauri-plugin-sql 2.4.0 等编译通过，1m24s，✅。
  - `pnpm tauri dev` 烟测：vite ready（localhost:1420）+ `Finished dev profile in 57.71s` + `Running target/debug/nook` 窗口弹出 + vite 检测并优化 `element-plus/es` 依赖（证明运行时按需引入生效）✅。后停掉 dev 进程（kill nook/vite/pnpm tauri dev，端口 1420 释放）。
- **改动文件清单**：`src-tauri/Cargo.toml`、`src-tauri/src/lib.rs`、`src-tauri/capabilities/default.json`、`vite.config.ts`、`tsconfig.json`、`src/main.ts`、`src/App.vue`、`src/stores/index.ts`（新）、`src/router/index.ts`（新）、`src/views/HomeView.vue`（新）、`auto-imports.d.ts`（生成）、`components.d.ts`（生成）、`package.json`、`pnpm-lock.yaml`。

## harness 规则扩充：接入 frontend-design skill

- **改了什么**：`AGENTS.md` 加硬约束 #15（设计前端页面前必须先用 Skill 工具调用 `frontend-design` skill）+ 专题文档表 style.md 行补"frontend-design skill 流程与调和"；`rules/style.md` 顶部新增「设计流程：frontend-design skill」段，写明调用流程（Purpose/Tone/Differentiation + 质量门槛）与「Nook 白色极简约束的调和」子段（PRD 优先级 > skill：冲突时以白色极简为准，skill 的"明暗变化/别做 AI slop"落到白色基底上而非靠深色）。
- **为什么**：用户要求设计前端页面时使用 frontend-design skill 并接入 harness，确保后续 feat-002~012 的 UI 不沦为套版"AI slop"；同时 PRD 白色极简是硬底线，须在规则层写清 skill 与 PRD 的优先级调和，避免 agent 被 skill 的"主题变化"带偏做深色。
- **验证结果**：文档级改动，无构建。`check-records.sh` 只扫 src/ + src-tauri/src 代码文件 mtime，本次仅改 .md，基线不受影响。
- **改动文件**：`AGENTS.md`、`rules/style.md`。

## 数据库文件名改名 app.db → nook.db

- **改了什么**：数据库文件名统一从 `app.db` 改为 `nook.db`（更语义、更品牌化）。涉及 `Database.load("sqlite:nook.db")` 及所有引用。改动文件：`PRD产品需求文档.md`（存储位置表 + 备份导入流程段，`app\.db`→`nook\.db`）、`README.md`（数据存储表）、`rules/tauri-ipc.md`、`rules/db.md`、`feature_list.json`（feat-003/feat-008 描述）。`app_setting` 表名保留不动（是 generic 表名，非文件名）。
- **为什么**：用户指出 `app.db` 太泛，`nook.db` 更合理且与产品名一致。feat-003 未做，目前无代码引用 `app.db`，纯文档改名零风险。
- **验证结果**：全量 grep 确认无 `app.db`/`app\.db` 拋留，`nook.db` 全部到位。文档级改动，无构建。
- **改动文件**：`PRD产品需求文档.md`、`README.md`、`rules/tauri-ipc.md`、`rules/db.md`、`feature_list.json`。

## harness 规则扩充：Vue 3 Composition API 规范

- **改了什么**：新建 `rules/vue.md`（Vue 3 官方推荐 Composition API 工程化写法：`<script setup>` 强制、响应式 ref/computed/watch、类型化 `defineProps<{}>()`/`defineEmits<{}>()`/`defineModel`、composable `useXxx` 抽离门槛、命名与目录、禁 any）。`AGENTS.md` 加硬约束 #16（禁 Options API）+ 专题文档表加 vue.md 行；`CLAUDE.md` 必做第 2 步 rules 列表加 vue。
- **为什么**：用户要求"组件工程化、遵循 Vue 3 官方推荐的 Composition API 写法"并写进 harness，统一团队（含未来 agent）写法，避免 Options API / 字符串式 props/emits / 相对路径 import 等。
- **验证结果**：文档级，无构建。feat-002 全部组件已按此规范写（`<script setup>` + 类型化 props + `@` 别名）。
- **改动文件**：`rules/vue.md`（新）、`AGENTS.md`、`CLAUDE.md`。

## harness 规则扩充：企业级开发范式 + 组件化

- **改了什么**：
  - `rules/vue.md` 加「组件化开发范式（企业级）」段：单一职责 / 展示组件vs容器组件（dumb 不调 api，smart 持数据）/ props-down-events-up（禁子组件改 props，用 defineModel）/ 插槽优先于 prop 传模板 / 复用门槛 ≥2 / defineExpose 收敛 / 组合优于继承。
  - `rules/project-structure.md` 加「企业级分层与依赖方向」段：view→composable→api→plugin 单向分层（下层不引上层）/ 组件不裸跑 SQL、不直接 invoke（api 层唯一出口）/ Pinia 状态归属 / 类型集中 / 常量与枚举提取（禁魔法字符串）/ 不静默吞错 / 不可变数据（取→改→写回）。
  - `AGENTS.md` 加硬约束 #17（禁跨层反向依赖）+ 专题文档表 project-structure 行补"企业级分层"、vue 行补"组件化范式"。
- **为什么**：用户要求把"企业级开发范式、组件化开发"写进 harness，统一分层与组件边界纪律，避免后续 feature 出现 view 里裸跑 SQL、子组件直 mutate props、跨层反向 import 等架构债。
- **验证结果**：文档级，无构建。check-records 仅扫代码 mtime，未改代码基线不受影响。
- **改动文件**：`rules/vue.md`、`rules/project-structure.md`、`AGENTS.md`。

## 窗口尺寸调大

- **改了什么**：`src-tauri/tauri.conf.json` 窗口配置从 `800×600` 调为 `1200×800`，加 `minWidth:960` / `minHeight:640`（防侧边栏被挤塌）、`resizable:true`、`center:true`（居中启动）、标题 `nook`→`Nook`。
- **为什么**：用户要求窗口调大；带侧边栏+内容区+顶栏的桌面管理应用 800×600 偏挤，1200×800 更舒适，最小宽 960 保证侧边栏 224 + 内容区可用宽度。
- **验证结果**：`cargo check`（tauri-build 解析 conf）3.69s ✅；`pnpm tauri dev` 烟测：窗口以新尺寸启动、无配置/运行时错误 ✅。后停 dev 进程。
- **改动文件**：`src-tauri/tauri.conf.json`。

## feat-002 基础布局（侧边栏 + 内容区 + 顶部搜索）

- **改了什么**：
  - **设计方向（frontend-design skill，硬约束 #15）**：定调 "Quiet Stationery" 暖纸极简——暖白纸面色（`#fbfbf9`/`#f5f3ee`）、墨色文字、发丝分隔替代重阴影、唯一仪式金强调色（`#b08a3e`，克制用于 active/品牌/宇宙订单卡）、衬线品牌字标（New York→Georgia 栈）+ 系统 CJK 无衬线正文（离线，禁网络字体 #9）、暖纸微颗粒纹理。在白色极简内做出有品味的非套版设计。
  - **离线图标**：装 `@iconify-json/lucide`（devDep），`main.ts` 用 `addCollection(lucide)` 注册——@iconify/vue 默认运行时从 API 拉图标=网络请求，违反 #9，改全程离线。
  - **design tokens**：新建 `src/assets/styles/tokens.css`——色板/字体/间距/动效/滚动条/选中态 CSS 变量集中，`main.ts` 引入。
  - **布局壳**：`src/App.vue` 改为左侧栏 + 右侧（顶栏搜索 + 内容区 RouterView）flex 布局。
  - **侧边栏** `src/components/layout/AppSidebar.vue`：品牌字标（Nook + 金点）+ 主导航（首页/待办/笔记/订单，Iconify lucide 线性图标）+ 发丝分隔线 + 设置项；激活态=仪式金左竖条 + 墨色 + 淡底；入场 staggered 淡入（`--i` 错峰，尊重 prefers-reduced-motion）。
  - **顶栏搜索** `src/components/layout/AppTopbar.vue`：发丝下划线款搜索框（非 Element Plus 默认方框），P1 占位（实时过滤在 feat-012）。
  - **页面外壳** `src/components/layout/PageShell.vue`：类型化 `defineProps<{title,subtitle?,placeholder?}>()` + 默认插槽，各模块页复用标题区。
  - **路由表** `src/router/index.ts`：五路由 `/ /todo /note /order /settings`，懒加载 `() => import()`，`meta.title`。
  - **占位页**：`src/views/home/`、`todo/`、`note/`、`order/`、`settings/` 各一个 `<script setup>` 占位页用 PageShell；删 feat-001 的 `src/views/HomeView.vue`（迁到 `views/home/`）。
- **为什么**：feat-002 是 PRD「整体架构与布局」——所有业务模块的视觉地基；前置 feat-001 已完成可开工。调 frontend-design skill（#15）避免套版"AI slop"，但白色极简（#10）优先级 > skill，故方向在白底内做文章而非靠深色/渐变。
- **验证结果**：
  - `pnpm build`（vue-tsc + vite build）：50 modules，533ms，✅ 无类型错误；路由按模块 code-split（TodoView/NoteView/HomeView/OrderView/SettingsView/PageShell 各自 chunk）。index chunk 698KB / gzip 133KB（lucide 1844 图标离线打包，V1 可接受）。
  - `cargo check`：0.66s，✅（feat-002 未动 Rust）。
  - `pnpm tauri dev` 烟测：vite ready（localhost:1420，286ms）+ nook 编译（4.20s）+ `Running target/debug/nook` 窗口弹出，无运行时 panic/error ✅。后停掉 dev 进程，端口 1420 释放。
- **改动文件**：`package.json`、`pnpm-lock.yaml`（+@iconify-json/lucide）、`src/main.ts`、`src/App.vue`、`src/router/index.ts`、`src/assets/styles/tokens.css`（新）、`src/components/layout/AppSidebar.vue`（新）、`src/components/layout/AppTopbar.vue`（新）、`src/components/layout/PageShell.vue`（新）、`src/views/home/HomeView.vue`（重写）、`src/views/todo/TodoView.vue`（新）、`src/views/note/NoteView.vue`（新）、`src/views/order/OrderView.vue`（新）、`src/views/settings/SettingsView.vue`（新）；删除 `src/views/HomeView.vue`。
